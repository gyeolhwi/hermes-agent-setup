# Hermes 설정 저장소용 에이전트 지침

이 저장소의 목적은 **새 환경에서 Hermes Agent를 쉽게 설치하고 필요한 기능만 안전하게 연결하도록 돕는 것**이다.

## 작업 순서

1. 사용 목적과 설치 위치(PC, WSL2, 원격 서버)를 먼저 확인한다.
2. 기존 Hermes·SSH·systemd 상태를 읽기 전용으로 점검한다.
3. 설치와 모델 연결을 진행한다.
4. Telegram, Slack, IMAP, Gateway 등은 사용자가 원할 때만 연결한다.
5. 모델 응답·메시지 수신·메일함 목록처럼 실제 동작으로 검증한다.

## 비밀값

- 토큰, 비밀번호, OAuth 코드, SSH 개인키를 출력·복사·기록하지 않는다.
- 비밀값 입력이 필요하면 사용자가 직접 입력하도록 멈춘다.
- `.env`, `auth.json`, `state.db`, 세션·로그 파일을 Git에 넣지 않는다.

## 예시 프로필

`examples/` 아래 내용은 특정 조직 또는 과거 환경의 참고 사례다. 사용자가 명시적으로 요청하지 않는 한 기본 설치에 적용하지 않는다.
