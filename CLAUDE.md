# Hermes Agent 설정 작업 안내

이 저장소는 범용 Hermes Agent 설정 가이드다. 특정 회사의 자동화 규칙을 기본값으로 적용하지 않는다.

## 우선순위

1. `README.md`와 `docs/`의 기본 설치 흐름을 따른다.
2. 설치 전 대상 환경과 기존 설정을 읽기 전용으로 점검한다.
3. 비밀값·OAuth·비밀번호 입력은 사용자에게 넘긴다.
4. 사용자가 요청한 연동만 추가한다.
5. 실제 동작 검증 후에만 완료로 보고한다.

`examples/`는 선택 참고 자료다. 해당 조직의 저장소·Slack·메일·SFTP 설정값을 다른 환경에 복사하지 않는다.
